<%if current_user != nil%>
<h1>joined demo's 📆 </h1>
  <%@realbookmark[0..4].each do |bookmark|%>
    <%= link_to demonstration_path(bookmark.demonstration) do%>
      <p class = "card"> The name is <%= bookmark.demonstration.title%> and start date is <%= bookmark.demonstration.start_time.strftime("%A, %d %B %Y")%>
    <%end%>
  <%end%>
  <%=link_to "Your bookmarks", bookmarked_path, class: "card"%>
  <%if @mydemonstrations != nil%>
  <%= link_to "Your Demostrations", mydemonstrations_path, class: "card"%>
  <%end%>
<%end%>

<h1>Recommended 🔥</h1>
  <% @demo[0..4].each do |demo|%>
    <%= link_to demonstration_path(demo) do%>
      <p class = "card"> The name is <%= demo.title%> and start date is <%= demo.start_time.strftime("%A, %d %B %Y")%>
    <%end%>
  <%end%>
📆
